pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "VilniusObjectRecognizer"
include(":androidApp")
include(":shared")